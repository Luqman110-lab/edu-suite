# Production Environment Variables Template
# Copy this to your deployment platform's environment variables section

# Database Connection
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Session Secret (Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Setup Key (required to register the first admin user)
SETUP_KEY=your-setup-key-change-this

# Node Environment
NODE_ENV=production

# Web Push (VAPID) Keys
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key

# Optional: Email Configuration (if using Resend)
# RESEND_API_KEY=your-resend-api-key
# RESEND_FROM_EMAIL=noreply@yourdomain.com
